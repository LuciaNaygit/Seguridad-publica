{
  "name": "Seguridad-publica",
  "version": "1.0.0",
  "description": "Proyecto final",
  "main": ".eleventy.js",
  "scripts": {
    "install": "npm install @11ty/eleventy --save-dev",
    "start": "npx @11ty/eleventy --serve --port=7777",
    "clean": "rm -rf docs/assets docs/*.md docs/*.njk",
    "deploy": "npx @11ty/eleventy --pathprefix=/",
    "deploy-subdomain": "npx @11ty/eleventy --pathprefix=/Seguridad-publica/"
  },
  "keywords": [
    "11ty",
    "eleventy",
    "github pages"
  ],
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
